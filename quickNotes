This line of code tells the browser to load all html <strong>prior<prior> to loading javascript
$(document).ready(main);

Dollar Sign - prefix variables for jquery, so it does not get confused with other things like integers
$

JQuery allows for:

Events - respond to user interactions

DOM Manipulation - modify HTML

Effects - animations
